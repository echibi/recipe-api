{% extends "base.twig" %}

{% block content %}
	<div class="admin recipe-list">
		{% include "partials/page-header.twig" with { 'title' : 'Ingredients' } %}
		<ul class="list-group">
			<li class="list-group-item list-group-item-info">
				<a href="{{ path_for("admin.edit-ingredient", {id: 'create'}) }}" class="btn btn-success">Create new</a>
			</li>
			{% for item in categories %}
				<li class="list-group-item">
					<a href="{{ path_for("admin.edit-ingredient", {'id': item.id}) }}">{{ item.id }} - {{ item.name }}</a>
					<button data-url="{{ path_for("admin.delete-ingredient", {'id' : item.id}) }}"
					        class="delete is-danger is-medium delete-recipe"></button>
				</li>
			{% endfor %}
		</ul>
	</div>
{% endblock %}