{% extends "base.twig" %}

{% block title %}{{ title }}{% endblock %}
{% block content %}
	{% include "partials/page-header.twig" with { 'title' : title } %}
	<form id="edit-ingredient" method="post" action="{{ post_url }}" enctype="multipart/form-data">
		<div class="field{{ form_errors.name ? ' is-danger' : '' }}">
			<label class="label" for="name">Namn</label>
			<input name="name" type="text" class="input" id="name" value="{{ old.name }}">
			{% if form_errors.name %}
				<span class="help is-danger">{{ form_errors.name | first }}</span>
			{% endif %}
		</div>
		<div class="field">
			<label class="label" for="slug">Slug</label>
			<input name="slug" type="text" class="input" id="slug" value="{{ old.slug ? old.slug : ingredient.slug }}" disabled>
		</div>
		<div class="field">
			<label class="label" for="image1">Huvudbild (TODO)</label>
			{% if ingredient.image1 %}
				<div class="main-image">
					<img src="{{ image_url(ingredient.image1, 'large') }}" alt="" />
				</div>
			{% endif %}
			<div class="box">
				<input type="file" class="form-control-file" id="image1" name="image1" aria-describedby="fileHelp">
			</div>
		</div>
		<button type="submit" class="button is-primary">Spara</button>
	</form>
{% endblock %}