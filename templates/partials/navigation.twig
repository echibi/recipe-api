<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
	<button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item{% if menu.active == "home" %} active{% endif %}">
				<a class="nav-link" href="{{ path_for("admin.index") }}">Home</a>
			</li>
			{% if auth.check %}
				<li class="nav-item{% if menu.active == "recipes" %} active{% endif %}">
					<a class="nav-link" href="{{ path_for("admin.list-recipes") }}">Recipes</a>
				</li>
				<li class="nav-item{% if menu.active == "categories" %} active{% endif %}">
					<a class="nav-link" href="{{ path_for("admin.list-categories") }}">Categories</a>
				</li>
				<li class="nav-item{% if menu.active == "ingredients" %} active{% endif %}">
					<a class="nav-link" href="#">Ingredients</a>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>